<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray1"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingBottom="80dp">

    <ImageView
        android:id="@+id/iv_home_logo_gray"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/all_logo_gray"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_home_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:includeFontPadding="false"
        android:text="@string/home_recipe_empty"
        android:textAppearance="@style/Bold.Body4"
        android:textColor="@color/gray4"
        app:layout_constraintEnd_toEndOf="@id/iv_home_logo_gray"
        app:layout_constraintStart_toStartOf="@id/iv_home_logo_gray"
        app:layout_constraintTop_toBottomOf="@id/iv_home_logo_gray" />

</androidx.constraintlayout.widget.ConstraintLayout>